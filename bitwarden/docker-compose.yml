version: '3'
services:
  bitwarden:
    image: vaultwarden/server
    container_name: bitwarden
    env_file: .env
    user: '1000'
    ports:
      - 3010:80
    restart: unless-stopped
    volumes:
      - ~/Data/docker/bitwarden:/data/
