apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jayherron-ingress
  namespace: default
spec:
  ingressClassName: nginx
  # tls:
  # - hosts:
  #     - jayherron.org
  #   secretName: jayherron-cert
  rules:
  # - host: www.jayherron.org
  #   http:
  #     paths:
  #     - pathType: Prefix
  #       path: "/"
  #       backend:
  #         service:
  #           name: service1
  #           port:
  #             number: 80
  - host: recipes.jayherron.org
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: mealie
            port:
              number: 80