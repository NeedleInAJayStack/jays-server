apiVersion: v1
kind: Service
metadata:
  name: zeroclaw
  namespace: zeroclaw
spec:
  selector:
    app: zeroclaw
  ports:
  - name: gateway
    port: 42617
    targetPort: 42617
    protocol: TCP
  type: ClusterIP
