apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
spec:
  selector:
    app: openclaw
  ports:
  - name: gateway
    port: 18789
    targetPort: 18789
    protocol: TCP
  - name: canvas
    port: 18793
    targetPort: 18793
    protocol: TCP
  type: ClusterIP
