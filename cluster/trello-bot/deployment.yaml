apiVersion: apps/v1
kind: Deployment
metadata:
  name: trello-bot
  namespace: trello-bot
  labels:
    app: trello-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trello-bot
  template:
    metadata:
      labels:
        app: trello-bot
    spec:
      containers:
        - name: website
          image: ghcr.io/needleinajaystack/trello-bot@sha256:614f853e389983b737067b74f23ebd4e40bade75034c84845b7fd9ac8af6224b
          ports:
            - containerPort: 3333
          env:
            - name: TRELLO_DEV_KEY
              valueFrom:
                secretKeyRef:
                  name: secret
                  key: devKey
            - name: TRELLO_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: secret
                  key: botToken
