apiVersion: stackgres.io/v1
kind: SGCluster
metadata:
  namespace: waterlog
  name: cluster
spec:
  postgres:
    version: '18'
  instances: 1
  pods:
    persistentVolume:
      size: '10Gi'
  configurations:
    sgPostgresConfig: postgres-config
    observability:
      prometheusAutobind: true
